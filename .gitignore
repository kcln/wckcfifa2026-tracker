data/cache/
*.pyc
__pycache__/
venv/
.venv/
.env

# ml engine (isolated venv + large raw dataset; models are committed)
ml/.venv/
ml/data/historical/*
!ml/data/historical/.gitkeep

# live-mode continuation flag (consumed by the workflow, never committed)
.live-continue
.live-continue
.season-ended

# retrain staging (promoted artifacts are committed; staging never is)
ml/data/models/staging/
